import 'package:flutter/material.dart';

/// History view.
/// 
/// This view contains a list of the trainings the user completed.
/// The user is able to CRUD the different trainings. **(to be implemented)**
class HistoryView extends StatelessWidget {
  const HistoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        actions: [],
      ),
    );
  }
}
